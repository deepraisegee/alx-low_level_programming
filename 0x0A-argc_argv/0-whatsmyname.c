#include <stdio.h>

/**
  * main - prints the file name
  * @argc: integer
  * @argv: pointer
  *
  * Return: Always 0
  */
int main(int argc __attribute__ ((unused)), char *argv[])
{
	printf("%s\n", argv[0]);

	return (0);
}
